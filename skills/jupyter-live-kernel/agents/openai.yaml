interface:
  display_name: "Persistent Jupyter"
  short_description: "Inspect live notebooks and run kernel code."
  default_prompt: "Use $persistent-jupyter to inspect a local JupyterLab or Jupyter Server instance, list live notebook sessions, read saved notebook contents, and execute code incrementally in an existing kernel."
